const quizApp = () => {
    let score = 0;

    if ("JavaScript" === "JavaScript") {
        score++;
    }

    if ("let" === "let") {
        score++;
    }

    if ("console.log()" === "console.log()") {
        score++;
    }

    console.log("Score:", score);
};

quizApp();